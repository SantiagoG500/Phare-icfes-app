<form class="section section--exam">
  <h1>{name}</h1>
  {#each setQuestions as setQuestion}
    <h2>{setQuestion.description}</h2>

    <section>
      {#each setQuestion.questions as set}
        <p>{set.question}</p>
        <ul class="questions">
          {#each set.answers as answer}
            <!-- <li>{answer}</li> -->
            <li class="questions__question">
              <label class="answer-label">
                <input
                  type="radio"
                  value={answer}
                  name={set.question}
                  data-question-id={`${set.questionId}`}
                  data-group-id={`${set.groupId}`}
                  />
                  
                {answer}
              </label>
            </li>
          {/each}
        </ul>
      {/each}
    </section>
  {/each}
</form>
